## Replay viewer for Lux Challenge 2021

* Requires the replay to exist in the "stateful" format.
* Requires electron (e.g. try `npm install -g electron`)
* Then do `electron .` in the Optics directory.
